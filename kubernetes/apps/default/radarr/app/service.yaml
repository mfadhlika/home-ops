apiVersion: v1
kind: Service
metadata:
  name: radarr
  labels:
    app.kubernetes.io/instance: radarr
    app.kubernetes.io/name: radarr
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/instance: radarr
    app.kubernetes.io/name: radarr
  ports:
    - name: http
      protocol: TCP
      port: 7878
      targetPort: http
